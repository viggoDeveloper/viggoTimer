<table>
    <thead>
        <tr>
            <th>Nombre</th>
            <th>Apellido</th>
            <th>Documento</th>
            {/* Agrega más encabezados para cada campo en tu colección */}
        </tr>
    </thead>
    <tbody>
        {data.map((item) => (
            <tr key={item.id}>
                <td>{item.name}</td>
                <td>{item.lastname}</td>
                <td>{item.document}</td>
                {/* Agrega más celdas para cada campo en tu colección */}
            </tr>
        ))}
    </tbody>
</table>

problema de worpresdd para mostras los precios a clientes fuera de colombia

